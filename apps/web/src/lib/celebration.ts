/**
 * Temporary Independence Day celebration switch.
 *
 * All tricolour UI (banner, header stripe, homepage section, pricing, cart,
 * footer, theme-color) is gated behind this constant. Set to `false` to
 * disable the celebration without touching any component.
 */
export const INDEPENDENCE_DAY_ACTIVE = true;

/** Promo code advertised by the celebration UI (created in prod admin). */
export const INDEPENDENCE_DAY_PROMO_CODE = "INDIA30";

/** Sale headline used across promotion surfaces. */
export const INDEPENDENCE_DAY_HEADLINE = "Independence Day Sale";

/** Sale copy — keeps the discount figure in a single place. */
export const INDEPENDENCE_DAY_DISCOUNT = "up to 30% off";
